"use client";

/**
 * Ambient music is owned by SoundProvider (shared element for autoplay unlock).
 * This component remains as a no-op mount point for backwards compatibility.
 */
export function AmbientMusic() {
  return null;
}
